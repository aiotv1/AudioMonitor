# كيفية الاستخدام - Audio Monitor

## ⚡ التشغيل السريع

### الخطوة 1: تثبيت المكتبات
قم بتشغيل `install.bat` أو اكتب في المحطة الطرفية:
```
pip install -r requirements.txt
```

### الخطوة 2: تجربة البرنامج
```
python audio_monitor.py
```

### الخطوة 3: إنشاء ملف EXE
قم بتشغيل `build.bat` أو اكتب:
```
pip install pyinstaller
pyinstaller --onefile --windowed --name=AudioMonitor --clean audio_monitor.py
```

ستجد الملف في: `dist\AudioMonitor.exe`

---

## 🎯 كيف يعمل البرنامج

1. البرنامج يعمل في شريط المهام (بجانب الساعة)
2. عندما يصدر أي تطبيق صوت، تتغير الأيقونة لتعرض أيقونة ذلك التطبيق
3. البرنامج يختار التطبيق الذي يصدر أعلى صوت
4. يمكنك النقر بزر الماوس الأيمن على الأيقونة للوصول إلى القائمة

---

## ❓ حل المشاكل

### البرنامج لا يكتشف الصوت؟
- تأكد من أن التطبيق الذي يصدر الصوت غير مكتوم
- البرنامج يحتاج بضع ثوان للكشف عن التطبيقات الجديدة

### لا تظهر أيقونة في شريط المهام؟
- تأكد من أنك قمت بتثبيت جميع المكتبات المطلوبة
- جرب إعادة تشغيل البرنامج

---

**ملاحظة:** البرنامج يعمل فقط على نظام Windows 🪟
